import React from 'react'
import { Sparkles } from '@react-three/drei'

const Particle = () => {
        <Sparkles
            count={100}
            speed={0.3}
            opacity={1}
            color="white"
            size={2}
            scale={5}
            noise={1} />
}
export default Particle;